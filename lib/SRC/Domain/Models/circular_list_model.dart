class CircularListModel {
  final String productLink;
  final dynamic productID;
  final dynamic productImage;
  final dynamic viewStatus;

  CircularListModel(
      this.productID, this.productLink, this.productImage, this.viewStatus);
}
