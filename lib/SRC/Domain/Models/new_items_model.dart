class NewItemsModel {
  final dynamic imageAddress;
  final dynamic productId;
  final dynamic productLink;
  final String productDescription;
  final double productPrice;

  NewItemsModel(this.productId, this.imageAddress, this.productDescription,
      this.productLink, this.productPrice);
}
