class MyOrderModel {
  final dynamic imageAddress;
  final dynamic productDescription;
  final dynamic orderNumber;
  final dynamic dateOfPurchase;

  MyOrderModel(this.orderNumber, this.imageAddress, this.productDescription,
      this.dateOfPurchase);
}
