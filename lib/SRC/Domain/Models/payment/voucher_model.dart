class VoucherModel {
  final dynamic validityDate;
  final String voucherName;
  final dynamic voucherDiscount;
  final dynamic imagePath;

  VoucherModel(this.validityDate, this.voucherDiscount, this.voucherName,
      this.imagePath);
}
